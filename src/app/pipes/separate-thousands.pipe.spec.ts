import { SeparateThousandsPipe } from './separate-thousands.pipe';

describe('SeparateThousandsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateThousandsPipe();
    expect(pipe).toBeTruthy();
  });
});
