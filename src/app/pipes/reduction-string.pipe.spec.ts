import { ReductionStringPipe } from './reduction-string.pipe';

describe('ReductionStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ReductionStringPipe();
    expect(pipe).toBeTruthy();
  });
});
